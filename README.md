# 🛡️ 灾害应急指挥与救援资源调度平台

基于 **Vue 3 + Vite + 高德地图 JS API v2.0** 的应急指挥演示系统。

## 功能特性

- **高德地图核心交互**
  - 受灾范围叠加（`Polygon`）、事件覆盖物（`Marker` 脉冲气泡）、人口密度热力图层（`Heatmap`）
  - 多类事件（洪涝/地震/火灾/山体滑坡/台风）按严重等级区分样式
  - 内建地图控件（缩放/比例尺/鹰眼）
- **事件管理**：搜索、类型/等级/状态筛选、事件详情、处置状态流转、处置时间线
- **资源调度**：多个资源库/救援点库存，拖拽式派发物资到受灾点，自动在地图绘制派发路线、估算距离与到达时间（直线 × 路网折算的演示算法）
- **物资派发闭环**
  - 分批签收：现场可多次签收，记录签收回执（数量/签收人/时间），在途量随每批签收逐次递减
  - 数量分账：每条派发独立核算 在途 / 实收 / 短缺 / 退回 四本账，需求清单按「实收+在途」计保障量、按实收单独回写
  - 短缺补派：签收时可同批认定短缺，缺口自动重新释放；一键按短缺量就近补派（库存不足跨基地拆单），补派单回链原单并防重复补派
  - 退回入库：在途余量可分批原路退回、库存回补，不再计入保障量；超量签收/退回与已办结记录的重复操作一律拦截
  - 撤回留账：撤回不再删除派发记录，在途（或挂起待续派）余量原路回库，已发生的签收/短缺认定/补派/退回回执与实收账目全部保留；撤回的短缺补派单会冲回原单已补派量、缺口重新释放；撤回单标记为「已撤回」退出保障量、道路阻断评估与地图路线，且不可再签收/退回/补派
  - 全程联动：灾点需求条与统筹缺口实时重算、安置点补给按在途/实收分账核算、道路阻断仅评估在途余量（部分签收后挂起/改派只动在途部分）、地图办结撤线、大屏新增「今日签收 / 短缺待补」
  - 向后兼容：无闭环字段的历史派发记录按全量在途处理，可直接进入新闭环流程
- **安置点按日补给**
  - 按实际入住时段核算：以「补给结算日」为周期，按各成员当日入住/转出时刻折算人日（如 08:00 入住计 2/3 人日），本日消耗 = 人日 × 人均系数实时滚动
  - 耐用品与消耗品分账：消耗品（食品/饮用水/医疗）按日消耗逐日累计；耐用品（帐篷）按在住峰值一次性配备、留在安置点周转，人员转出不回溯、不按日耗
  - 跨日结转：「日结」将当日消耗入账，结余库存（实收 − 已耗）与在途物资自动滚存至次日，日结记录（人日/日终在住/消耗/结转快照）全程留痕
  - 缺口联动重算：人员转出、分批签收、短缺补派、退回/撤回均同步重算缺口（缺口 = 累计需求 − 实收 − 在途），历史欠账跨日滚存
  - 避免重复补给：无闭环字段的历史派发记录按全量在途计入保障量，一键补给仅按当日核算缺口调拨，缺口平后自动拦截
- **多灾点资源统筹**
  - 按 灾情等级 → 需求缺口 → 运输时长 自动生成跨基地分配方案（预占不扣库存）
  - 方案支持人工调整（改数量 / 换基地自动重算时长），冲突项实时高亮
  - 提交时统一校验并锁定库存，库存冲突自动按运输时长重新分配，仍不足的缺口如实反馈
  - 批量派发联动更新事件状态与时间线、地图路线、大屏统计；需求清单实时显示 已派/需求 进度
- **群众转移安置**
  - 围绕灾情事件建立转移批次，指挥员分配车辆（占用资源库车辆库存）与安置点（预占床位）
  - 现场三环节登记：接运 → 入住 → 转出，支持单人（姓名/证件号）与批量两种模式
  - 重复登记检测：本批次重复直接拦截；跨批次重复可一键「改派」人员；批次级支持改派安置点/车辆
  - 床位容量联动：入住校验剩余床位（本批次预占自动释放），超容量提示改派
  - 未完成批次按人员分组拆分：勾选成员（在途/在住，已转出不参与）携带全部登记历史（接运/入住/转出时间戳）拆为独立批次；新分组可分别安排车辆（从基地库存新占，原批车辆保留）与安置点（含已入住成员锁定原安置点，跨点按剩余床位校验），计划人数随人切分
  - 拆分联动：床位预占按各批未入住名额重分配、新分组运输路线独立重算 ETA 并即时纳入生效阻断评估（绕行/改派/挂起）；两批按各自登记进度重新推导状态与办结条件（满员全转出仍自动办结回收车辆）；事件转移进度与指挥大屏按人汇总守恒，操作留痕事件时间线，新批次带 `splitFrom` 溯源
  - 安置点物资按日补给核算（人日折算 + 耐用品/消耗品分账 + 跨日结转），一键按当日缺口就近调拨（生成补给派发记录）
  - 全部转出自动办结并回收车辆；进度实时回写事件详情、处置时间线与指挥大屏统计
- **道路阻断处置**
  - 现场人员上报封闭范围：地图圈画多边形（单击标点、右键/双击完成）或一键「快捷上报」当前事件运输走廊
  - 影响评估：自动检出路线穿越封闭区（含与封闭区边界共线/贴边行走）的在途物资派发与转移批次，指挥员勾选确认
  - 方案生成：逐条给出 联合绕行（多处生效阻断一次绕清，含里程/时长增量）→ 改派（换基地/换安置点，自动避开全部生效阻断）→ 挂起 候选，可逐条调整、一键执行
  - 执行前复核：执行瞬间按最新库存/床位与全部生效阻断重新校验；原方案失效（库存被占用、新阻断封堵走廊）时自动降级为绕行→改派→挂起
  - 跨阻断联动：一处处置（绕行/改派/挂起）后，其它生效阻断上的同一任务自动销项或重开待处置；新建派发/批次即时纳入评估
  - 执行同步：地图路线（绕行途经点）、到达时间（ETA 重算）、车辆与床位占用（改派联动释放/预占）实时更新
  - 无路可走（目的地/出发地落入封闭区）时挂起任务：派发物资退回基地、不计入已满足量；批次保留车辆床位预占并暂停登记
  - 恢复通行后：绕行路线在剩余生效阻断视角下联合重排（能回直则回直，仍需绕行则重排），挂起任务一键续派（续派前复核路线与库存，仍受其它阻断影响的保持挂起）
- **道路抢修工单闭环**
  - 由阻断记录发起抢修：指挥员从资源基地分配抢修队伍（人员）、车辆与物资，派单即扣减库存；同一阻断只允许一张在途工单
  - 现场接单并上报进度：进度单调递增，可多次延期（阻断继续保留）
  - 完工上报实际消耗：道路仍封闭、进入待验收；验收通过才解除封闭，并在剩余生效阻断视角下重算全部受影响运输（绕行回直/重排、ETA 重算、挂起任务保持不变）
  - 延期或失败保留阻断：抢修失败 / 验收不通过均不解除封闭，可对同一阻断重新派单
  - 撤单与完工按实际消耗归还：撤单、失败、办结统一结算「派单量 − 实际消耗」的剩余人员/车辆/物资回库，结算幂等
  - 强制恢复联动：指挥员直接恢复通行时，抢修中的工单自动撤单、待验收工单视同验收通过办结；地图阻断标记实时显示抢修状态与进度
- **指挥大屏**：在报事件、等级分布、今日派发、道路阻断、抢修中/待验收、**在效预警/预警待确认**、在途转移、安置中、受影响群众等实时统计，支持「实时模拟」自动刷新数据
- **实时预警协同**（气象 / 水文 / 地质数据接入）
  - 多源信号接入：省气象台、水文监测中心、地质灾害监测网、地震台网信号统一接警（实时流模拟 WebSocket/SSE 推送，也可单条模拟或人工录入），按「种类 + 落区」归并同一灾害过程，报文级去重；同过程后续信号自动新发 / 升级 / 降级 / 解除
  - 按灾情等级多角色告警：蓝色→值班调度员，黄色→+现场指挥员，橙色→+气象地灾专家，红色→全员（含应急指挥长）；各角色逐一签收确认、越权与重复签收拦截，全员确认后预警转「已确认」
  - 确认升级：监测信号升级或专家/指挥长人工研判升级后，确认进度按新等级重置并重新组织告警；降级时收窄告知范围、已确认结论保留
  - 撤销解除：解除信号或人工解除通知全部已告知角色；橙/红预警自动建档且未启动处置的灾情事件联动结案，已处置事件仅回写时间线
  - 联动回写：预警落区就近匹配在报事件（超范围无匹配时橙/红自动建档灾情事件），事件等级随预警抬升、处置状态随确认/预置出库流转、全流程写入事件处置时间线
  - 预置调度：红/橙预警确认后按等级需求模板就近生成预置方案，一键出库实际派发（库存不足项如实反馈），回写资源调度、地图路线与指挥大屏
  - 地图大屏：预警影响落区（六边形虚线范围）与等级标记叠加，标记角标显示角色确认进度（脉冲=待确认，✓=已确认），点击聚焦查看；大屏新增「在效预警 / 预警待确认」统计卡
  - 复盘留痕：接入、确认、升级、解除、建档、预置出库每个动作沉淀复盘帧，预警状态/等级/确认进度支持任意节点回放还原，回放只读期拦截信号接入
- **历史复盘与节点回放**（事件溯源）
  - 全程留痕：事件流转、物资派发（含签收/补派/退回/撤回/挂起/改派/绕行）、群众转移（建批/登记/拆分/改派）、道路阻断（上报/评估/处置/恢复）、抢修工单（派单/进度/验收/结算）每个动作自动沉淀一帧全量态势快照，并汇总该帧新增的事件时间线、阻断处置、抢修工单三类日志
  - 按时间回放：时间轴逐节点步进 / 拖动游标 / 1×·2×·4× 自动播放；seek 到任意帧即整体还原当时的事件状态、派发四本账、库存占用、床位占用、阻断区与绕行路线，地图路线/阻断区/安置点角标与全部面板同步重绘
  - 四维度复盘详情：🔁状态变化、🗺️路线调整（里程/时长/途经点/基地或安置点变更）、🏗️资源占用（各基地库存与安置点床位增减）、📝处置日志；可按事件/派发/转移/阻断/抢修分类筛选
  - 任意节点分叉恢复演练：从任一历史帧「分叉新分支」继续推演，**原演练分支原样保留不截断**；分支树支持多层分叉与兄弟分支并存
  - 多分支切换推演：随时在各分支末端态势间切换、沿选定分支继续处置；各分支独立维护库存、床位、派发四本账、转移批次、阻断与抢修状态（帧内全量快照天然隔离，互不污染）
  - 分支处置结果对照：任选两条分支（默认当前分支 vs 主干），对齐共同祖先后比对末端态势——事件状态、各基地库存、安置点床位、派发状态/账目、转移批次、阻断、抢修工单逐维度列出差异，并可一键跳到任一分支继续推演
  - 向后兼容：旧版单线 `frames` 历史（含已持久化数据）经 `loadLegacyFrames` 自动归一化为单主干分支，回放/分叉语义不变，首次分叉即升级为分支树
  - 回放只读锁定：复盘期间主工作区业务操作全部拦截并显示只读遮罩，保证历史快照不被污染
- **多灾情场景**：预置多种 mock 场景（典型洪涝、汛期多点并发），一键切换

## 快速开始

```bash
# 1. 安装依赖
npm install

# 2. 准备高德 key —— 复制样板文件为 .env 并填入自己的 key
copy .env.example .env
#   编辑 .env：
#   VITE_AMAP_KEY=你自己的高德key
#   （可选）VITE_AMAP_SECURITY_CODE=你的jscode安全密钥

# 3. 启动开发服务器
npm run dev
# 打开 http://localhost:5173
```

> 仓库已通过 `.gitignore` 忽略 `.env`，key 不会进入版本库。`.env.example` 为公开样板。

## 项目结构

```
disaster-command/
├── .env.example          # 环境变量样板（git 提交）
├── .gitignore            # 忽略 node_modules / .env / dist 等
├── vite.config.js
├── index.html
└── src/
    ├── main.js           # 入口
    ├── App.vue           # 三栏布局（事件 / 地图 / 调度+详情）
    ├── style.css
    ├── config/
    │   └── amap.js       # 高德 Loader 封装 + 读 .env key
    ├── mock/
    │   ├── data.js       # 灾情场景 / 事件 / 资源库 / 安置点 / 预警种类与角色配置 mock 数据
    │   └── warningFeed.js# 气象/水文/地质/地震实时监测信号流（模拟 WebSocket/SSE 接入）
    ├── store/
    │   ├── command.js    # Pinia 全局状态、派发与流转逻辑（派发闭环：分批签收/短缺补派/退回入库，含派发挂起/改道/改派/续派）
    │   ├── transfer.js   # 转移安置：批次、登记、床位与物资联动（含批次挂起/绕行/续派；安置点按日补给：人日折算/分账/日结结转）
    │   ├── roadblock.js  # 道路阻断：上报、影响评估、方案生成执行、恢复续派
    │   ├── repair.js     # 道路抢修工单：派单扣资源/接单进度/延期失败/验收解除封闭/按实际消耗结算归还
    │   ├── warning.js    # 实时预警协同：信号接入定级、多角色告警/确认升级/解除、事件建档联动、预置调度出库
    │   └── replay.js     # 历史复盘：action 包装录制全量快照、时间轴回放只读锁定、帧间四维度差异、多分支树（分叉保留原线/切换续写/末端结果对照/旧单线兼容）
    ├── utils/
    │   └── geo.js        # 几何工具：点在区内判定、路线穿越检测、绕行路径生成
    └── components/
        ├── CommandHeader.vue   # 顶栏 + 场景切换 + 大屏统计（含预警统计）+ 历史复盘入口
        ├── EventList.vue       # 左侧事件列表 + 筛选
        ├── MapBoard.vue        # 高德地图渲染层（Polygon/Heatmap/Marker/路线/安置点/阻断区/预警落区/圈画/回放快照重绘）
        ├── DispatchPanel.vue   # 资源调度面板（单点派发 / 统筹方案 / 转移安置 / 道路阻断 / 道路抢修 / 实时预警页签）
        ├── PlanPanel.vue       # 多灾点统筹：方案生成、人工调整、批量派发
        ├── TransferPanel.vue   # 转移安置：批次建派、接运/入住/转出登记、安置点补给
        ├── RoadBlockPanel.vue  # 道路阻断：上报表单、影响确认、方案执行、挂起续派
        ├── RepairPanel.vue     # 道路抢修：派单分配、接单、进度、延期、完工验收、消耗归还
        ├── WarningPanel.vue    # 实时预警：数据源/实时接入、多角色确认、升级解除、事件建档、预置调度
        ├── ReplayPanel.vue     # 历史复盘：时间轴、播放控制、状态/路线/资源/日志四维度详情、分叉恢复
        └── EventDetail.vue     # 事件详情 + 状态流转 + 转移进度 + 时间线
```

## 测试

```bash
npm test   # 转移安置 + 批次拆分 + 道路阻断（单阻断/多处阻断联合避障）+ 物资派发闭环 + 安置点按日补给 + 道路抢修工单 + 历史复盘 + 实时预警协同核心逻辑回归：建批/查重/床位/车辆/按人员分组拆分（登记历史保留/床位车辆路线联动/办结条件与进度同步）/阻断评估/联合绕行/改派/挂起续派/方案执行前复核/跨阻断联动/分批签收/短缺补派/退回入库/在途实收分账/旧记录兼容/防重复签收回库/按入住时段折算人日/耐用品与消耗品分账/跨日结转库存与在途/转出签收补派退回联动重算缺口/防重复补给/抢修派单扣减与重复派单拦截/接单进度单调/延期失败保留阻断/完工实际消耗登记/验收解除封闭并重算运输/撤单失败按实际消耗归还/结算幂等/强制恢复自动办结/复盘全量快照录制与无效动作去帧/任意节点 seek 精确还原（库存/路线/账目/登记/工单）/回放只读锁定/多分支分叉保留原线不截断/分支切换续写与库存床位派发抢修独立/多层分支树/分支末端处置结果对照/旧单线 frames 历史兼容/信号接入去重归并/按等级多角色告警与越权重签拦截/全员确认/升级重置确认/降级收窄/解除联动结案/就近匹配与橙红自动建档/事件提级与时间线回写/预置调度一键出库防重复/预警动作入帧与回放快照还原
```

## 后续可扩展方向

- 实时预警推送接入真实 WebSocket / SSE 数据源（替换 `src/mock/warningFeed.js` 模拟信号流）
- 多级权限与账号体系（RBAC，对接预警协同四角色）
- 资源库存与供应联动、自动补货
- 复盘记录导出 / 分享（时间轴快照 JSON、复盘报告）
- 天气 / 地质灾害**真实图层**叠加（当前预警落区为按影响半径生成的演示多边形）
- 预警确认超时督办 / 升级短信与语音外呼、移动端指挥适配
- 移动端指挥适配

## ⚠️ 安全提示

高德 key 直接写在前端 `.env` 中构建进产物，是**公开可见**的，且有每日调用配额。
- 仅适合本地开发 / 个人演示。
- 部署到生产或公开服务前，请替换为**限定域名**的正式 key，或通过**服务端代理**调用 Web 服务 API。
- 切勿将 `.env` 提交到公开仓库。